<snippet>
	<content><![CDATA[
define([
	'require',
	'signals',
	'json!./config.json',
	'superclasses/mediator',
	${1:null}
], function (require, Signal, config, Mediator, ${2:null}) {
	var mediator = Mediator.extend({

		config: config,
		modules: {},
		moduleConstructors: [],

		init: function (settings) {
			this.addModules($2);

			this._super(settings);
		},

		resume: function () {
			this._super();

			// Resume code
		},

		signals: {
			Loaded: new Signal()
		}
	});

	return mediator;
});
]]></content>
	<tabTrigger>component</tabTrigger>
</snippet>
